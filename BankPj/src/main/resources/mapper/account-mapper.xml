<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="account">
	<insert id="accountSign"
		parameterType="kr.co.bk.model.vo.Account">
		insert into account (a_no, a_account, a_pw, m_no, a_type, a_money)
		values (account_seq.nextval, #{a_account}, #{a_pw}, #{m_no}, #{a_type},0)
	</insert>

	<select id="accountAsk" resultType="String">
		select a_type from account where m_no = #{m_no} and a_type = #{a_type} 
	</select>
</mapper>
